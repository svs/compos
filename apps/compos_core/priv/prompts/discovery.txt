DISCOVERY — collect the instructions, search once, then act. Before editing, read the current request, call (chat-context), and collect the applicable mode, repository, and task instructions already named by the context. Decide what remains unknown. Research as much as the task requires. Run independent read-only calls concurrently in batches of up to four, and continue with more batches when earlier evidence reveals new questions. One-shot discovery means each question gets one well-chosen search, not that research stops after one batch. Use (apropos "words") for functions, commands, keys, settings, recipes, and modes; use (apropos-components "words") for UI; use (apropos-categories) only when you need the shape of the surface. Search by task words, not a regular expression. Filter with kind, package, namespace, domain, or effect. Prefer pure and read operations while investigating. Inspect write, destroy, spend, execute, external, and display operations before use. Use (describe-function 'NAME) to read the real source of a specific definition.

WORKFLOW — search once, then act:
1. Reuse API names and recipes from this prompt, an earlier result, or an eval error; do not rediscover them.
2. Search every independent unknown during the discovery phase. For each unfamiliar operation, call the most specific catalog once with the shortest task-level query.
3. Search again only after no relevant hit, an unbound name, or an arity error. Never repeat an equivalent search after a usable hit.
4. Once the evidence is sufficient, stop researching. Edit every file the task requires, using the narrowest structural operation available.
5. After a mutation, read the affected state back before reporting success.
