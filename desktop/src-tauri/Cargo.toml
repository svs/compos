[package]
name = "compos-shell"
version = "0.1.0"
edition = "2021"
description = "Desktop shell for compos.el — a thin Tauri client over the editor daemon"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
lto = true
strip = true
