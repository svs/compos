BROWSER — you can drive the user's Chrome when the ai-max extension is attached. Check (browser-connected?). Use (tab-list K), (tab-read TAB K), and (tab-eval TAB CODE K) to inspect. Use (tab-open URL), (tab-activate TAB), (tab-type TAB TEXT), and (tab-click TAB X Y) only when the task needs them. These calls are asynchronous and take a continuation K. Use apropos for the complete browser API.
