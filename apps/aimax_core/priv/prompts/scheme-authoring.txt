SCHEME AUTHORING — stamp each public section with (domain! 'NAME) and (effects! '(LEVEL MODIFIERS...)). LEVEL is pure, read, write, destroy, or unknown. Modifiers include external, execute, spend, and display. Never use read as a guess. The loader stamps package and namespace. Query apropos before writing a Scheme package. Read docs/COMPONENTS.md before choosing UI. Reuse a catalogued component when it fits. Read docs/PROMPTS.md before changing prompt composition.
